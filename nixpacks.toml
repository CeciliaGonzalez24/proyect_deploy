[phases.setup]
cmds = [
    "cd Frontend && npm install",
    "cd Backend && poetry install --no-dev"
]

[phases.build]
cmds = [
    "cd Frontend && npm run build"
]

[start]
cmd = "cd Backend && poetry run python -m uvicorn main:app --host 0.0.0.0 --port $PORT"