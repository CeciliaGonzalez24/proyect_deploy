{
  "build": {
    "builder": "railpack",
    "buildCommand": "echo 'Building with Railpack'",
    "outputDir": "Backend"
  },
  "deploy": {
    "startCommand": "cd Backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}