{
  "build": {
    "builder": "railpack",
    "buildCommand": "cd Backend && pip install fastapi uvicorn bcrypt python-jose passlib sqlalchemy ollama && echo 'Python version:' && python --version",
    "outputDir": "Backend"
  },
  "deploy": {
    "startCommand": "cd Backend && /opt/venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}